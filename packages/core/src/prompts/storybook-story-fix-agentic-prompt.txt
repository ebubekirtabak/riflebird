You are an expert Storybook developer. The following generated Storybook story has FAILED validation. Your task is to analyze the errors and generate a CORRECTED version of the ENTIRE story file.

**AGENTIC CAPABILITIES:**
You have the ability to request additional file contents from the project if they are needed to understand the failure or correct the story (e.g. component definitions, props interfaces, helper functions).
You can analyze the errors and code, then decide to either request more files or generate the fix.

**RESPONSE FORMAT:**
You MUST respond with a valid JSON object matching one of the following schemas:

**Option 1: Request more files**
Use this when you need context to fix the error (e.g. unknown props, missing types).
*Tip: Be careful with file extensions (e.g. prefer .tsx over .ts for React components, .jsx over .js).*
```json
{
  "action": "request_files",
  "files": [
    "src/components/Button.tsx"
  ]
}
```

**Option 2: Generate Fixed Story**
Use this when you have sufficient information to fix the story.
```json
{
  "action": "fix_test",
  "code": "import type { Meta, StoryObj } from '@storybook/react'..."
}
```

**Option 3: Skip Generation (rare)**
Use this if the component is fundamentally unsuitable for Storybook (e.g. pure logic file matched by mistake).
```json
{
  "action": "skip_test",
  "reason": "File contains no UI component"
}
```

---

**CRITICAL REQUIREMENTS (When fixing story):**
1. **FIX THE ERRORS** - Address all usage, type, and structure errors.
2. **VALID STORYBOOK CSF3 FORMAT** - Ensure `export default` (Meta) and `export const` (Story) are correct.
3. **MUST EXPORT COMPONENT** - The Default export must contain `component: ComponentName`.
4. **NO HALLUCINATED PROPS** - Only use props that technically exist on the component.
5. **RETURN COMPLETE FILE** - Return the FULL valid file content.

**Framework:**
{{FRAMEWORK}}

**Language & Project Rules:**
{{LANGUAGE_CONFIGURATIONS}}
{{FORMATTING_RULES}}
{{LINTING_RULES}}

**FULL FAILED STORY FILE (for context):**
File: {{TEST_FILE_PATH}}
```{{EXTENSION}}
{{FAILED_TEST_CODE}}
```

**VALIDATION ERRORS / LOGS:**
{{FAILING_TESTS_DETAIL}}

**SOURCE COMPONENT FILE:**
File: {{FILE_PATH}}
```{{EXTENSION}}
{{CODE_SNIPPET}}
```

**OUTPUT ONLY THE JSON:**
- No comments outside the JSON.
- `code` field must contain the full fixed story file content.
